spring:
  application:
    name: Dept_Service_Basic_DB
  
  datasource:
    url: jdbc:postgresql://localhost:5432/dept_emp_db 
    username: postgres
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8081	